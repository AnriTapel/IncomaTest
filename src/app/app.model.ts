export interface DataTableItem {
    product: string;
    mark: string;
    seller: string;
    region: string;
    customer: string;
}

export interface FilterOption {
    name: string;
    isApplied: boolean;
}